<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TourGuideAPP.Views.RegisterPage"
             BackgroundColor="{StaticResource Secondary}"
             Title="Đăng ký">

    <ScrollView>
        <VerticalStackLayout Padding="32" Spacing="16" VerticalOptions="Center">

            <!-- Header -->
            <VerticalStackLayout Spacing="4" Margin="0,40,0,30">
                <Label Text="Tạo tài khoản"
                       FontSize="30"
                       FontAttributes="Bold"
                       TextColor="{StaticResource Primary}"
                       HorizontalOptions="Center"/>
                <Label Text="Tham gia cộng đồng TourGuide"
                       FontSize="14"
                       TextColor="{StaticResource Muted}"
                       HorizontalOptions="Center"/>
            </VerticalStackLayout>

            <!-- Full Name -->
            <Frame BackgroundColor="{StaticResource Surface}" CornerRadius="12" Padding="0" BorderColor="{StaticResource Gray100}">
                <Entry x:Name="FullNameEntry"
                       Placeholder="Họ và tên"
                       PlaceholderColor="{StaticResource Muted}"
                       TextColor="{StaticResource OnSurface}"
                       BackgroundColor="Transparent"
                       Margin="16,12"/>
            </Frame>

            <!-- Email -->
            <Frame BackgroundColor="{StaticResource Surface}" CornerRadius="12" Padding="0" BorderColor="{StaticResource Gray100}">
                <Entry x:Name="EmailEntry"
                       Placeholder="Email"
                       PlaceholderColor="{StaticResource Muted}"
                       TextColor="{StaticResource OnSurface}"
                       BackgroundColor="Transparent"
                       Keyboard="Email"
                       Margin="16,12"/>
            </Frame>

            <!-- Password -->
            <Frame BackgroundColor="{StaticResource Surface}" CornerRadius="12" Padding="0" BorderColor="{StaticResource Gray100}">
                <Entry x:Name="PasswordEntry"
                       Placeholder="Mật khẩu (ít nhất 6 ký tự)"
                       PlaceholderColor="{StaticResource Muted}"
                       TextColor="{StaticResource OnSurface}"
                       BackgroundColor="Transparent"
                       IsPassword="True"
                       Margin="16,12"/>
            </Frame>

            <!-- Confirm Password -->
            <Frame BackgroundColor="{StaticResource Surface}" CornerRadius="12" Padding="0" BorderColor="{StaticResource Gray100}">
                <Entry x:Name="ConfirmPasswordEntry"
                       Placeholder="Xác nhận mật khẩu"
                       PlaceholderColor="{StaticResource Muted}"
                       TextColor="{StaticResource OnSurface}"
                       BackgroundColor="Transparent"
                       IsPassword="True"
                       Margin="16,12"/>
            </Frame>

            <!-- Error label -->
            <Label x:Name="ErrorLabel"
                   Text=""
                   TextColor="#E94560"
                   FontSize="13"
                   HorizontalOptions="Center"
                   IsVisible="False"/>

            <!-- Success label -->
            <Label x:Name="SuccessLabel"
                   Text=""
                   TextColor="#4CAF50"
                   FontSize="13"
                   HorizontalOptions="Center"
                   IsVisible="False"/>

            <!-- Register button -->
            <Button Text="Đăng ký"
                    Clicked="OnRegisterClicked"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="White"
                    CornerRadius="12"
                    HeightRequest="52"
                    FontAttributes="Bold"
                    Margin="0,8,0,0"/>

            <!-- Back to login -->
            <HorizontalStackLayout HorizontalOptions="Center" Spacing="4">
                <Label Text="Đã có tài khoản?"
                       TextColor="{StaticResource Muted}"
                       FontSize="14"
                       VerticalOptions="Center"/>
                <Label Text="Đăng nhập"
                       TextColor="{StaticResource Tertiary}"
                       FontSize="14"
                       FontAttributes="Bold"
                       VerticalOptions="Center">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnLoginTapped"/>
                    </Label.GestureRecognizers>
                </Label>
            </HorizontalStackLayout>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>