@{ ViewData["Title"] = "Không có quyền truy cập"; Layout = null; }
<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Không có quyền truy cập</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="d-flex align-items-center justify-content-center min-vh-100 bg-light">
  <div class="text-center">
    <div style="font-size:80px">🚫</div>
    <h2 class="fw-bold mt-3">Không có quyền truy cập</h2>
    <p class="text-muted">Trang này chỉ dành cho <strong>Chủ quán</strong> và <strong>Admin</strong>.</p>
    <a asp-controller="Auth" asp-action="Logout" class="btn btn-danger mt-2">
      Đăng xuất và thử lại
    </a>
  </div>
</body>
</html>